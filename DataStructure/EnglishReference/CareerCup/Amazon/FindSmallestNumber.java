/*
    Givena number A, find the smallest number which has only 1s and 0s
    as its digits which divisible by the number A. For example, if the
    given number A is 4, the smallest number with 1s and 0s is which
    divisible by 4 is 100
*/

import java.util.*;
import java.lang.*;

class FindSmallestNumber {
    /*  This function is used to transfer a binary number to integer
        For example: 5(101) -> 101
    */
    private int BinaryToInteger(int binary) {
        int power = 0;
        int result = 0;        

        for (i = 0; i < 32; i++) {
            if (binary )
        }
    }

    static public int findSmallestNumber(int A) {
        //1, 2, 3, 4, 5, 6, 7, 8, 9
        int x = 0;
        while(1) {
            x++;
        
        }
    }
}
